# 下一位操作者从这里继续

## 当前交付不是机器人已跑通

内容20期已编排；程序绘制示意封面20张；GPT Image实际图片0张，提示词20份。没有新增摄像头、GPU策略、舵机或IMU实测。之前用户日志确认GLFW缺少DISPLAY，本包把第5期标为blocked。

## 开始顺序

打开publisher.html看EP01，然后读posts/01/checklist.md和docs/RUNBOOK.md的L01。先补ZERO 3W、相机和排线的丝印照片与系统版本。相机型号尚未知，不预先选择overlay。GPU侧平行排查EP05的图形会话，解决后再验证EP07接口与EP08参考推理。

每期第1天实验，第2天复测并发布。首轮起始2026-09-17可用build --start-date整体平移；这不是一套已经创建的定时任务。

## 真实照片回来时

原图放evidence/raw/对应期数，原文件不改动。复制一份到evidence/public/，人工检查密钥、网络标识、私人环境与他人面孔，发布使用公开副本。保存原始证据哈希；没有用户审阅，不自动提交原图到公开仓库。可临时在publisher.html选择图片预览，不会上传。

只把确有证据的结论补进content/series.py正文，重新build，测试，然后git commit。正文里保留“参考策略推理/自训/仿真/真机”的边界。

## 待完成项

1. 全部实际硬件/训练验证；没有凭内容完成给实验打勾。
2. 真正生成20张GPT Image插画，并审图、添加AI示意识别。现有技术PNG可先用，但不冒充GPT图。
3. 根据实际板版次确认电源、IMU与通信方案。没有可靠接线证据不输出针脚级接线指令。
4. 轨迹采集/批量评测接入实际上游环境。提供的是数据合同、校验器和接入位置指引，不是已经接好的训练补丁。

## 修改原则

主源是content/series.py，网页/TXT/SVG/合订MD由build生成；status.json独立保留。不要只改生成文件然后让下次build覆盖。不要把带密钥的日志、ONNX权重、训练视频、上游CAD直接堆入此公开仓库。
