# IMU坐标与时序合同

请填写[hardware_inventory.md中的frame_contract](hardware_inventory.md#frame_contract)，避免维护两份相互冲突的坐标定义。

对应EP17与[RUNBOOK L17](RUNBOOK.md#l17imu)。记录板坐标、机身坐标、变换方向、单位、四元数顺序、采样时钟、接收时钟及超时策略。未验证字段为unknown，不得直接用于实机闭环。
